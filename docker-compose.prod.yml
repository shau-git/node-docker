version: "3"
services: 
  nginx: 
    ports:
      - "80:80"
  node-app:
    build: 
      context: . # the location of the docker file
      args: 
        NODE_ENV: production 
    environment:
      - NODE_ENV=production
    command: node index.js